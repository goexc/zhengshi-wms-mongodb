export enum Types {
  empty = "",
  primary = "primary",
  success = "success",
  warning = "warning",
  danger = "danger",
  info = "info",
  text = "text",
}

export enum Sizes {
  large = "large",
  default = "default",
  small = "small",
}
