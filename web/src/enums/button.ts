export enum Types {
  primary = "primary",
  success = "success",
  warning = "warning",
  danger = "danger",
  info = "info",
  text = "text",
  empty = "",
}

export enum Sizes {
  large = "large",
  default = "default",
  small = "small",
}
